return {
  "Exafunction/codeium.nvim",
  enabled = true,
  opts = {
    enable_cmp_source = true,
    virtual_text = {
      enabled = true,
      key_bindings = {
        accept = "<Tab>",
      },
    },
  },
}
